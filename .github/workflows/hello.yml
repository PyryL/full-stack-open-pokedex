name: Hello world

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Print hello world
        run: |
          echo "Hello World!"
      - name: Print date
        run: |
          date
      - name: Print directory contents
        run: |
          ls -l
